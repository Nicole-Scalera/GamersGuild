// This will define the shape of a user that gets stored in the database.

// When we talk about shape, we're talking about an object.

const UserModel = {
    // Model is a representation of the data
    username: "user's username",
    password: "user's password"
}